// Input: 
// STR = FLIPKARTISHIRING
// ROW = 4

// Output: FRRLATIIIKIHNPSG

// grid[x][y]
const grid = Array.from({length: 4})
let charIdx = 0
while (charID>STR.length)
    for (let i = 0; i<ROW ; i++) {
        grid[0][i] = STR[charIdx]
        charIdx++;
    }
    for(let i = 0 ; i<ROW-1;i++  {
        grid[i-]
    })
    
